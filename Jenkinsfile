pipeline {
  agent any
  stages {
    stage('compile') {
      steps {
        bat 'echo compile hi'
      }
    }
    stage('utest') {
      steps {
        bat 'echo utest hi'
      }
    }
    stage('deploy') {
      steps {
        bat 'echo deploy hi'
      }
    }
  }
}